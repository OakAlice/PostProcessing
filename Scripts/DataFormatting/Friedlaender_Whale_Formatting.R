# Formatting the whale data ------------------------------------------------
# all in matlab format

files <- list.files(file.path(base_path, "Data", species, "raw"), full.names = TRUE, recursive = TRUE)

